<!-- index.html  |  NaturPrime (Demo) - Ana Sayfa
     Tek dosya: HTML + CSS + JS (responsive). Diğer sayfalar (catalog/product/cart) sonraki dosyalarda eklenecek. -->
<!doctype html>
<html lang="tr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>NaturPrime | Güç. Netlik. Performans.</title>
  <meta name="description" content="Maskülen, minimal ve güven veren alışveriş deneyimi. Öne çıkan ürünler, hızlı teslimat, kolay iade." />
  <meta name="theme-color" content="#0b0c10" />

  <style>
    :root{
      --bg:#0b0c10;
      --bg2:#0f1118;
      --card:#121522;
      --line:rgba(255,255,255,.10);
      --text:#e8ebf2;
      --muted:rgba(232,235,242,.70);
      --muted2:rgba(232,235,242,.55);
      --metal1:#f3f4f7;
      --metal2:#9aa1ad;
      --shadow: 0 18px 50px rgba(0,0,0,.45);
      --radius:16px;
      --radius2:24px;
      --max:1140px;
      --ok:#56d364;
      --warn:#f5c542;
      --focus:rgba(255,255,255,.16);
    }

    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji","Segoe UI Emoji";
      background:
        radial-gradient(1100px 600px at 18% 18%, rgba(255,255,255,.08), transparent 60%),
        radial-gradient(900px 500px at 80% 30%, rgba(255,255,255,.06), transparent 55%),
        linear-gradient(180deg, var(--bg) 0%, var(--bg2) 100%);
      color:var(--text);
      letter-spacing:.2px;
    }

    a{color:inherit; text-decoration:none}
    button{font:inherit}
    .container{max-width:var(--max); margin:0 auto; padding:0 18px}

    /* Top bar */
    .topbar{
      background:rgba(0,0,0,.25);
      border-bottom:1px solid var(--line);
      backdrop-filter: blur(10px);
      position:sticky; top:0; z-index:50;
    }
    .topbar-inner{
      display:flex; align-items:center; justify-content:space-between;
      min-height:68px;
      gap:14px;
    }

    .brand{
      display:flex; align-items:center; gap:10px;
      font-weight:800; letter-spacing:.8px; text-transform:uppercase;
    }
    .mark{
      width:36px; height:36px; border-radius:12px;
      background:
        radial-gradient(18px 18px at 30% 25%, rgba(255,255,255,.35), transparent 55%),
        linear-gradient(135deg, rgba(255,255,255,.18), rgba(255,255,255,.05));
      border:1px solid rgba(255,255,255,.16);
      box-shadow: 0 10px 30px rgba(0,0,0,.35);
      position:relative;
      overflow:hidden;
    }
    .mark:after{
      content:"";
      position:absolute; inset:-40%;
      background:linear-gradient(90deg, transparent, rgba(255,255,255,.22), transparent);
      transform: rotate(25deg);
      animation: shine 4.2s ease-in-out infinite;
    }
    @keyframes shine{
      0%,40%{transform:translateX(-40%) rotate(25deg); opacity:.0}
      55%{opacity:.8}
      80%,100%{transform:translateX(70%) rotate(25deg); opacity:0}
    }

    .nav{
      display:flex; align-items:center; gap:14px;
      color:var(--muted);
      font-weight:600;
    }
    .nav a{
      padding:10px 10px;
      border-radius:12px;
      border:1px solid transparent;
    }
    .nav a:hover{
      color:var(--text);
      border-color:rgba(255,255,255,.10);
      background:rgba(255,255,255,.04);
    }

    .actions{
      display:flex; align-items:center; gap:10px;
      margin-left:auto;
    }
    .search{
      display:flex; align-items:center; gap:10px;
      background:rgba(255,255,255,.04);
      border:1px solid rgba(255,255,255,.10);
      border-radius:14px;
      padding:10px 12px;
      min-width: 260px;
    }
    .search input{
      width:100%;
      border:none; outline:none;
      background:transparent;
      color:var(--text);
    }
    .search input::placeholder{color:rgba(232,235,242,.45)}
    .icon{
      width:18px; height:18px; opacity:.8; flex:0 0 auto;
    }

    .btn{
      border:1px solid rgba(255,255,255,.16);
      background:rgba(255,255,255,.06);
      color:var(--text);
      padding:10px 14px;
      border-radius:14px;
      font-weight:700;
      cursor:pointer;
      transition:.18s ease;
      display:inline-flex; align-items:center; gap:10px;
    }
    .btn:hover{transform: translateY(-1px); background:rgba(255,255,255,.08)}
    .btn:active{transform: translateY(0px)}
    .btn.primary{
      border-color: rgba(255,255,255,.22);
      background: linear-gradient(180deg, rgba(255,255,255,.14), rgba(255,255,255,.06));
    }
    .pill{
      font-size:12px;
      padding:2px 8px;
      border-radius:999px;
      border:1px solid rgba(255,255,255,.18);
      color:rgba(232,235,242,.85);
      background:rgba(0,0,0,.25);
    }

    /* Hero */
    .hero{
      padding:56px 0 34px;
    }
    .hero-grid{
      display:grid;
      grid-template-columns: 1.2fr .8fr;
      gap:18px;
      align-items:stretch;
    }
    .hero-card{
      background:
        radial-gradient(700px 420px at 12% 20%, rgba(255,255,255,.10), transparent 65%),
        linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.02));
      border:1px solid rgba(255,255,255,.12);
      border-radius: var(--radius2);
      box-shadow: var(--shadow);
      overflow:hidden;
      position:relative;
    }
    .hero-main{
      padding:34px;
      min-height: 320px;
    }
    .kicker{
      display:inline-flex; align-items:center; gap:10px;
      color:rgba(232,235,242,.80);
      font-weight:700;
      letter-spacing:.7px;
      text-transform:uppercase;
      font-size:12px;
    }
    .kicker .dot{
      width:8px; height:8px; border-radius:999px;
      background:rgba(255,255,255,.65);
      box-shadow: 0 0 0 5px rgba(255,255,255,.08);
    }
    h1{
      margin:14px 0 10px;
      font-size: clamp(30px, 4.2vw, 44px);
      line-height:1.06;
      letter-spacing:.2px;
    }
    .sub{
      margin:0 0 18px;
      color:var(--muted);
      font-size:16px;
      line-height:1.55;
      max-width: 52ch;
    }
    .hero-cta{
      display:flex; gap:10px; flex-wrap:wrap;
      margin-top:18px;
    }
    .hero-meta{
      display:flex; gap:16px; flex-wrap:wrap;
      margin-top:22px;
      color:var(--muted2);
      font-weight:600;
      font-size:13px;
    }
    .hero-meta span{
      display:inline-flex; align-items:center; gap:8px;
      padding:8px 10px;
      border-radius:999px;
      border:1px solid rgba(255,255,255,.10);
      background:rgba(0,0,0,.18);
    }
    .spark{
      width:10px; height:10px; border-radius:999px;
      background:rgba(255,255,255,.60);
      opacity:.9;
    }

    .hero-side{
      padding:22px;
      display:flex; flex-direction:column; gap:12px;
      min-height: 320px;
    }
    .side-box{
      border:1px solid rgba(255,255,255,.10);
      border-radius: var(--radius);
      background: rgba(0,0,0,.18);
      padding:16px;
    }
    .side-title{
      font-weight:800; letter-spacing:.4px; margin:0 0 6px;
    }
    .side-text{margin:0; color:var(--muted); line-height:1.55; font-size:14px}
    .side-row{
      display:flex; justify-content:space-between; gap:10px; align-items:center;
      margin-top:12px;
      padding-top:12px;
      border-top:1px solid rgba(255,255,255,.08);
      color:var(--muted2);
      font-weight:700;
      font-size:13px;
    }

    /* Sections */
    section{padding:26px 0}
    .section-head{
      display:flex; align-items:flex-end; justify-content:space-between;
      gap:14px;
      margin-bottom:12px;
    }
    .section-head h2{
      margin:0;
      font-size:18px;
      letter-spacing:.3px;
      text-transform:uppercase;
    }
    .section-head p{
      margin:0;
      color:var(--muted);
      font-weight:600;
      font-size:13px;
    }

    .grid{
      display:grid;
      grid-template-columns: repeat(12, 1fr);
      gap:14px;
    }

    /* Product cards */
    .p-card{
      grid-column: span 3;
      background: rgba(255,255,255,.03);
      border:1px solid rgba(255,255,255,.10);
      border-radius: var(--radius);
      overflow:hidden;
      box-shadow: 0 10px 28px rgba(0,0,0,.26);
      display:flex;
      flex-direction:column;
      min-height: 260px;
    }
    .p-img{
      height:140px;
      background:
        radial-gradient(140px 90px at 30% 30%, rgba(255,255,255,.12), transparent 60%),
        linear-gradient(135deg, rgba(255,255,255,.06), rgba(255,255,255,.02));
      border-bottom:1px solid rgba(255,255,255,.10);
      position:relative;
    }
    .p-badge{
      position:absolute; top:10px; left:10px;
      font-size:11px;
      padding:6px 10px;
      border-radius:999px;
      border:1px solid rgba(255,255,255,.16);
      background:rgba(0,0,0,.25);
      color:rgba(232,235,242,.86);
      font-weight:800;
      letter-spacing:.4px;
      text-transform:uppercase;
    }
    .p-body{padding:14px}
    .p-title{margin:0 0 6px; font-weight:900; letter-spacing:.2px}
    .p-desc{margin:0 0 10px; color:var(--muted); font-size:13px; line-height:1.45}
    .p-row{
      margin-top:auto;
      display:flex; justify-content:space-between; align-items:center;
      gap:10px;
      padding:14px;
      border-top:1px solid rgba(255,255,255,.10);
      background:rgba(0,0,0,.18);
    }
    .price{
      font-weight:900;
      letter-spacing:.2px;
    }
    .p-btn{
      border:1px solid rgba(255,255,255,.18);
      background:rgba(255,255,255,.07);
      color:var(--text);
      padding:10px 12px;
      border-radius:12px;
      font-weight:900;
      cursor:pointer;
      transition:.18s ease;
      white-space:nowrap;
    }
    .p-btn:hover{transform:translateY(-1px)}
    .p-btn:active{transform:translateY(0px)}

    /* Trust badges */
    .trust{
      grid-column: span 12;
      display:grid;
      grid-template-columns: repeat(12, 1fr);
      gap:14px;
    }
    .t-card{
      grid-column: span 3;
      border:1px solid rgba(255,255,255,.10);
      border-radius: var(--radius);
      background:rgba(0,0,0,.18);
      padding:16px;
    }
    .t-top{display:flex; align-items:center; justify-content:space-between; gap:10px}
    .t-title{margin:0; font-weight:900; letter-spacing:.2px}
    .t-note{margin:8px 0 0; color:var(--muted); font-size:13px; line-height:1.5}
    .chip{
      font-size:11px;
      padding:6px 10px;
      border-radius:999px;
      border:1px solid rgba(255,255,255,.14);
      background:rgba(255,255,255,.05);
      color:rgba(232,235,242,.85);
      font-weight:800;
      letter-spacing:.4px;
      text-transform:uppercase;
    }

    /* Reviews */
    .review{
      grid-column: span 4;
      border:1px solid rgba(255,255,255,.10);
      border-radius: var(--radius);
      background:rgba(255,255,255,.03);
      padding:16px;
    }
    .stars{letter-spacing:2px; color:rgba(255,255,255,.85)}
    .r-text{margin:10px 0 12px; color:var(--muted); line-height:1.55; font-size:14px}
    .r-foot{display:flex; justify-content:space-between; align-items:center; gap:10px}
    .r-name{font-weight:900}
    .r-tag{color:var(--muted2); font-weight:800; font-size:12px; text-transform:uppercase; letter-spacing:.4px}

    /* FAQ */
    .faq{
      grid-column: span 12;
      display:grid;
      grid-template-columns: repeat(12, 1fr);
      gap:14px;
    }
    details{
      grid-column: span 6;
      border:1px solid rgba(255,255,255,.10);
      border-radius: var(--radius);
      background:rgba(0,0,0,.18);
      padding:14px 14px;
    }
    summary{
      cursor:pointer;
      list-style:none;
      font-weight:900;
      letter-spacing:.2px;
    }
    summary::-webkit-details-marker{display:none}
    .faq p{margin:10px 0 0; color:var(--muted); line-height:1.55; font-size:14px}

    /* Footer */
    footer{
      padding:30px 0 40px;
      color:var(--muted);
      border-top:1px solid rgba(255,255,255,.10);
      margin-top:20px;
    }
    .foot-grid{
      display:grid;
      grid-template-columns: 1.2fr .8fr;
      gap:18px;
      align-items:start;
    }
    .foot-links{
      display:flex; gap:14px; flex-wrap:wrap;
      justify-content:flex-end;
      font-weight:800;
      letter-spacing:.3px;
      text-transform:uppercase;
      font-size:12px;
    }
    .foot-links a{
      padding:10px 12px;
      border:1px solid rgba(255,255,255,.10);
      border-radius:999px;
      background:rgba(0,0,0,.18);
    }
    .foot-links a:hover{background:rgba(255,255